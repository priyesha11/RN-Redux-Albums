import {StyleSheet} from 'react-native';

// Different background colors to be used in app
const background = StyleSheet.create({
    white:{
        backgroundColor:'white'
    }
});

export default background;